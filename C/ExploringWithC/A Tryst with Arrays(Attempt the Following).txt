1)
#include <stdio.h>
int print(int *,int);
int diff(int *,int);
void main()
{
int i,a[25],length=0;
for(i=0;i<25;i++){
printf("Enter at %d =",i+1);
scanf("%d",&a[i]);
if(a[i]==-1)
break;
length++;
}
printf("Count =%d\n",length);
printf("A =");
length = print(a,length);
printf("\nCount =%d\n",length);
diff(a,length);
printf("D1 =\n");
length =print(a,length-1);
printf("\nCount =%d\n",length);
diff(a,length);
printf("D2 =\n");
length =print(a,length-1);
printf("\nCount =%d\n",length);
diff(a,length);
printf("D3 =\n");
length =print(a,length-1);

printf("\nCount =%d\n",length);
}

int print(int *a,int length)
{
int i;
for(i=0;i<length;i++)
printf("%d ",a[i]);
return (length);
}

int diff(int *a,int length)
{
int i;
for(i=0;i<length;i++){
a[i]=a[i+1]-a[i];
}
}

2)
#include <stdio.h>
void main(){
int a[25],i,length=0,count=0,j=0,k=0;
for(i=0;i<25;i++){
printf("Enter %d =",i+1);
scanf("%d",&a[i]);
if(a[i]==-1)
break;
length++;
}

for(i=0;i<length-1;i++)
{
for(j=i;j<length-1;j++){
if(a[i]==a[j])
count++;
else {
i=j;
break;
}
}
printf("%d %d\n",a[i-1],count);
count=0;
count++;
}
}

3)
#include <stdio.h>
void main()
{
int i,j,a[3][3],f=0,diagonal=0,lower=0,upper=0,wrong=0;
for(i=0;i<3;i++){
  for(j=0;j<3;j++){
   printf("Enter at %d %d",i+1,j+1);
   scanf("%d",&a[i][j]);
   if(((i==j) && (a[i][j]!=0))||((i!=j) && (a[i][j]==0)))
   lower=1;
  }
}

for(i=0;i<3;i++){
  for(j=0;j<3;j++){
   if((j>i) && (a[i][j]==0) || (i>j) && (a[i][j]!=0))
   upper=1;
  }
}

for(i=0;i<3;i++){
  for(j=0;j<3;j++){
  if((i>j) && (a[i][j]==0) || (j>i) && (a[i][j]!=0))
  lower=1;
 }
}
printf("lower =%d",lower);
printf("upper =%d",upper);
printf("Diagonal =%d",diagonal);
}


4)
#include <stdio.h>
int  main()
{
int sales[5][10][7];
int i,j,k;
float Aofweeks=0,salesinweek=0,w_chips,w_city;
for(i=0;i<5;i++){
 printf("no. chips:\n");
 w_chips=0;
 for(j=0;j<10;j++){
  printf("No city:\n");
  printf("Sales day by day\n");
  for(k=0;k<7;k++){
   scanf("%d",&sales[i][j][k]);
   w_chips =w_chips+sales[i][j][k];
  }
 }
 printf("Chip no. %d sales =%f\n",i,w_chips);
 Aofweeks=Aofweeks+w_chips;
}
Aofweeks =Aofweeks/7;
printf("Average Daily sates =%f",Aofweeks);
for(i=0;i<10;i++){
 w_city=0;

 for(j=0;j<5;j++){
  for(k=0;k<7;k++)
  w_city =w_city+sales[i][j][k];
 }
 printf("City no. %d sales=%f\n",j,w_city);
}
return 0;
}
