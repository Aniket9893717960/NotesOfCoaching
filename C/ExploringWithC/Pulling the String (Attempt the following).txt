1)
#include <stdio.h>
void main()
{
char ch[]="Somewhere over the rainbow";
char ch1[]="over";
int i=0,j=0,start=-1;
while(ch[i]!='\0')
{
if(ch[i]==ch1[j])
{
 if(j==0)
 start=i+1;
 else if(ch1[j+1]=='\0'){
 printf("%s is found at %d position",ch1,start);
 break;
 }
  j++;
}
 else {
    if((start=i) && (ch1[j+1]!=ch[i+1])){
    start=-1;
    j=0;
    }
  }
i++;
}
if(start ==-1){
 printf("%s is not found in the string\n",ch1);
 }
}

2)
#include <stdio.h>
int encode(char *a);
int decode(char *a);

int main()
{
char ch[]="Man reach must always his grasp.... or what is the heaven for?";
int i=0;
while(ch[i]!='\0'){
printf("%c",ch[i++]);
}
printf("\nEncode this code\n");
encode(ch);
printf("\n%s",ch);
printf("\nDecode this code\n");
decode(ch);
printf("\n%s",ch);
return 0;
}

int encode(char *a)
{
int i=0;
 while(a[i]!='\0'){
  if(a[i]>='a' && a[i]<='z')
  a[i] = a[i]-43;
  i++;
 }
return 0;
}

int decode(char *a)  
{
int i=1;
 while(a[i]!='\0'){
   if(a[i]==' '){}
   else if(a[i]=='.' || a[i]=='?'){}
   else
   a[i] =a[i]+43;
   i++;
  }
return 0;
}


3)
#include <stdio.h>
int main()
{
char *str1[]={
"A FRiend Is.."
"SomEOne Loving & trUE..."
"fOr insTrance somEONE....."
"exactlY likE yoU !!"
};
char *currentString =str1[i];
int j=0,count=0;
 
while(currentString[j]!='\0'){
printf("%c",currentString[j]);
if((currentString[j]>=65) && (currentString[j]<=91))
count++;
j++;
}
printf("UpperCase =%d",count);
return 0;
}


4)       DOUBT
       ---------
#include <stdio.h>
int palindrome(char *,int,int);
int main()
{
char *str[]={
"MalayalaM",
"To really mess things up...",
"One needs to know C!!",
"able was I ere I saw  elba"
};
int j=0;
while(j<(sizeof(str)/sizeof(str[0])))
{
int i=0;
char *t=str[j];
 while(t[i]!='\0'){
 int start=0;
  if(t[i]==' '){
  start=i+1;
   if(palindrome(t,i-1,start)){
    for(int l=start;l<i;l++)
    printf("%c",t[l]);
   printf("\n");
   }
 }
 i++;
 }
  if(t[i]=='\0')
   if(palindrome(t,i-1,0)){
    for(int l=0;l<i;l++)
    printf("%c",t[l]);
   printf("\n");
    }
j++;
}
printf("\nProgram is end");
return 0;
}

int palindrome(char *ch,int end,int start){
while(start<end){
 if(ch[start]!=ch[end])
 return 0;
 start++;
 end--;
 }
if(start>end)
return 1;
}